<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinFormsApi.Views.SpeakPage">
  <StackLayout>
  <Grid RowSpacing="10" Padding="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="3*"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    
    <Entry x:Name="SpeakEntry" Grid.Row="0" Text="{Binding MainText}" VerticalOptions="Center" />
    <Button x:Name="SpeakButton" Grid.Row="1" HorizontalOptions="Center" VerticalOptions="Center" Text="Parlez !"/>
  </Grid>
  </StackLayout>
  
  
</ContentPage>